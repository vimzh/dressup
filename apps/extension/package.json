{
  "name": "@zdress/extension",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Chrome and Firefox extension source and packaging commands.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:chrome": "node scripts/build.mjs chrome",
    "build:firefox": "node scripts/build.mjs firefox",
    "package": "node scripts/build.mjs --zip",
    "lint:firefox": "npm run build:firefox && npx --yes web-ext@8 lint --source-dir=dist/firefox --self-hosted"
  }
}
